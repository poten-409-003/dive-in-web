export const signOut = async () => {
  "use server";
  console.log("signOut");
  // const supabase = createClient();
  // const { error } = await supabase.auth.signOut();
  // if (!error) {
  //   revalidatePath("/");
  //   redirect("/");
  // }
};
